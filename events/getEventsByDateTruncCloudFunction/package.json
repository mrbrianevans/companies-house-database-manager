{
  "name": "get-event-frequency-cloud-function",
  "version": "0.1.8",
  "scripts": {
    "build": "tsc",
    "deploy": "tsc && gcloud functions deploy eventsFrequency --region=europe-west1 && npm version patch"
  },
  "devDependencies": {
    "@types/node": "^14.14.28",
    "@types/pg": "^7.14.10",
    "typescript": "^4.1.5"
  },
  "dependencies": {
    "pg": "^8.5.1"
  }
}
