{
  "name": "save-company-events-cloud-function",
  "version": "0.0.2",
  "scripts": {
    "build": "tsc",
    "deploy": "tsc && gcloud functions deploy saveCompanyEvents --region=europe-west1 && npm version patch",
    "test": "tsc && node tester"
  },
  "devDependencies": {
    "@types/node": "^14.14.28",
    "@types/pg": "^7.14.10",
    "typescript": "^4.1.5"
  },
  "dependencies": {
    "pg": "^8.5.1",
    "request": "^2.88.2"
  }
}
